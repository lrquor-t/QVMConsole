- 采用 Wire 委托模式：在 `service` 根目录下通过 `_wire.go` 文件声明全局函数变量，由子模块在 `init()` 中赋值，以打破循环导入。
- 统一的异步任务注册：所有长耗时业务逻辑均封装为 `taskqueue.Handler`，在 `main.go` 中集中注册并绑定到具体的 `TaskType`。
- 配置优先级机制：系统配置遵循“环境变量 > 数据库持久化设置 > 代码默认值”的加载顺序，支持运行时动态调整部分参数。
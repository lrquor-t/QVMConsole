- 使用 `Hook` 前缀命名全局函数变量（如 `HookEnsureVMNotMigrating`），以标识其为可注入的依赖点。
- 在 `init()` 函数中执行依赖注入逻辑，确保在包加载时完成钩子注册，避免运行时 nil 指针调用。
- 对于存在循环依赖的类型转换，在根包中编写私有包装函数（如 `getTemplateMetaForVM`），在注册时进行类型适配。
- 使用 `FirstNonEmpty` 或类似的工具函数处理错误信息的 fallback 逻辑，优先保留具体的 stderr 输出。
- **Web 框架**：使用 `gin-gonic/gin v1.12.0` 构建高性能 HTTP 服务。
- **虚拟化交互**：集成 `digitalocean/go-libvirt` 进行原生 Libvirt RPC 通信，支持高效的 VM 管理。
- **持久化**：采用 `gorm.io/gorm` 配合 `sqlite` 驱动存储系统配置与元数据。
- **安全认证**：基于 `golang-jwt/jwt/v5` 实现 HS256 签名的 JWT 身份验证与 API Key 支持。